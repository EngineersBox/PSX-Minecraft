#include "cursor.h"

#include "../../util/preprocessor.h"

void uiCursorAction(VSelf, const DVECTOR* cursor_position, bool pressed) ALIAS("UICursor_action");
void UICursor_action(VSelf, const DVECTOR* cursor_position, bool pressed) {

}

void uiCursorRender(VSelf, RenderContext* ctx, Transforms* transforms) ALIAS("UICursor_render");
void UICursor_render(VSelf, RenderContext* ctx, Transforms* transforms) {

}