# Menu Designer

Visual designer for menus used in PSXMC. Allows for positioning and sizing of components such as buttons.
Provides automatic code generation to convert menu components and their attributes into corresponding
C code ready to be used directly.

## Running

```
uv sync
uv run main.py
```
